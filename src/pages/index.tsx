import Map from "@/stories/components/Map";
import type { NextPage } from "next";
import React from "react";

const Home: NextPage = () => {
    return (
        <React.Fragment>
            <Map />
        </React.Fragment>
    );
};

export default Home;
